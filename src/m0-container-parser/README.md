# m0 — Container parser

This milestone provides a minimal, dependency-free ISO-BMFF/HEIF box walker.

## Build

From repo root:

- `make build-m0`

## Run

- `./build/avif_boxdump testFiles/Link-U/fox.profile0.8bpc.yuv420.avif`

Options:
- `--max-depth N`
